# 基于不均衡小样本学习的光学遥感卫星陆上目标检测识别比赛 · 评分方案 V1.6 总结

> 来源:`比赛评分方案-V1.6.pdf`(上海卫星工程研究所)。本文件为评分方案的要点提炼,并纳入官方补充的"按三大类分别计算再平均"的口径说明。

---

## 1. 评分方案概述

评选分**初赛**与**决赛**两个阶段。

### 1.1 初赛 ——「刚性指标门槛制」,总分 100 分

| 项目 | 分值 | 说明 |
| --- | --- | --- |
| 性能指标基础分 | 70 分 | 必须先通过三项刚性指标门槛才能获得 |
| 方案合理性 | 10 分 | 技术路线完整性、实验设计合理性 |
| 技术创新程度 | 10 分 | 检测算法创新性及其对精度的提升效果 |
| 工程可落地性 | 10 分 | 实际场景适配性与长期运行可靠性 |

四项相加为初赛总分。**未达刚性指标者,初赛不予通过。**

### 1.2 决赛 —— 仅对通过初赛的作品评审,总分 100 分

| 项目 | 分值 | 考察重点(同初赛口径) |
| --- | --- | --- |
| 方案合理性 | 20 分 | 技术路线完整性、实验设计合理性 |
| 技术创新程度 | 50 分 | 算法独创性、精度提升效果 |
| 工程可落地性 | 30 分 | 实际部署潜力、长期运行可靠性 |

决赛依据技术独创性、实际部署潜力、技术路线严谨性等打分,择优确定揭榜团队。

---

## 2. 三项刚性指标(初赛门槛)

在赛题发布单位统一提供的**封闭测试集**上自动化评测,须同时满足:

| # | 刚性指标 | 要求 |
| --- | --- | --- |
| 1 | 时敏目标整体检测**召回率** | ≥ 85% |
| 2 | 时敏目标整体检测**虚警率** | ≤ 20% |
| 3 | 单幅 **10,000 × 10,000 像素**图像推理时间 | ≤ 20 秒 |

**测试硬件环境**:单张 NVIDIA RTX 3090 或同等算力的国产 GPU/NPU。

> 召回率、虚警率的评测方法见第 3 节;推理时间的口径见第 4 节。

---

## 3. 检测指标评价方式

### 3.1 目标大类与计算口径(★ 官方补充)

本比赛待检测目标分为**三个大类**:舰船、飞机、车辆。

- **初赛刚性指标**:对三个类别**不做区分**,三类目标**合并**计算召回率和虚警率即可。
- **官方补充(决赛/排序口径)**:**先分别计算三个大类各自的召回率和虚警率,再取三大类召回率的平均值、虚警率的平均值**,作为对应刚性指标要求的值。
  - 即:刚性指标中的"整体召回率/虚警率"在精细排名时按 $\text{Recall}_{\text{整体}} = \tfrac{1}{3}(\text{Recall}_{\text{船}} + \text{Recall}_{\text{飞机}} + \text{Recall}_{\text{车辆}})$、$\text{FDR}_{\text{整体}} = \tfrac{1}{3}(\text{FDR}_{\text{船}} + \text{FDR}_{\text{飞机}} + \text{FDR}_{\text{车辆}})$ 对齐。
  - 每个大类内部再分子型(例:船 = 驱护舰 / 航母 / 两栖船 / 民船 四型),先算各子型的召回率与虚警率,**子型召回率取平均 = 该大类召回率,子型虚警率取平均 = 该大类虚警率**;飞机、车辆同法。

### 3.2 召回率 / 虚警率公式

$$\text{Recall} = \frac{TP}{TP + FN}$$

$$\text{FDR}(\text{虚警率/误检率}) = \frac{FP}{FP + TP}$$

### 3.3 TP / FP / FN 定义(表 1)

| 名称 | 中文名称 | 定义 |
| --- | --- | --- |
| TP | 真正例 | 预测结果与某个真实目标成功匹配,且该真实目标此前未被其他预测框匹配 |
| FP | 假正例 / 虚警 | 预测报出了目标但未能匹配任何真实目标;或同一真实目标的重复检测框 |
| FN | 假负例 / 漏检 | 真实目标未被任何预测结果成功匹配 |

### 3.4 匹配计算细节

- 按预测**置信度 score 从高到低**排序后依次匹配。
- **每个预测框最多匹配一个真实框;每个真实框最多被一个预测框匹配。**
- 多个预测框匹配同一真实目标时,**置信度最高的计为 TP**,其余计为 FP。
- 未匹配到真实目标的预测框计为 **FP**;未被匹配的真实目标计为 **FN**。

### 3.5 匹配准则(IoU)

$$\text{Match}(p_i, g_j) = 1,\ \text{if}\ \text{IoU}(p_i, g_j) \ge \tau$$

$$\text{IoU} = \frac{\text{Area}(B_{\text{pred}} \cap B_{\text{gt}})}{\text{Area}(B_{\text{pred}} \cup B_{\text{gt}})}$$

其中 $p_i, B_{\text{pred}}$ 为预测框、$g_j, B_{\text{gt}}$ 为真值框。

### 3.6 IoU 阈值 τ(表 2)

| 目标类型 | IoU 阈值 τ |
| --- | --- |
| **车辆目标** | **0.35** |
| 其他目标(舰船、飞机等) | 0.50 |

---

## 4. 推理时间计算口径

> 推理时间 = 从**完成数据读取**到**输出推理结果**的时间,**不包含数据读取时间**。
>
> 单幅 10,000 × 10,000 像素图像须 ≤ 20 秒(RTX 3090 或同等算力国产 GPU/NPU)。

---

## 5. 方案合理性 / 技术创新程度 / 工程可落地性 打分规则

对通过刚性指标的团队,赛题发布单位组织行业专家,依据各队提交的**完整技术解决方案(研究报告 + 可运行算法代码 + 测试结果文件)** 进行打分。

### 5.1 排序依据 ——「7 个排名」二次排序

赛题发布单位先对各团队下列 7 项指标分别排序:

1. 船的检测召回率
2. 船的虚警率
3. 飞机的检测召回率
4. 飞机的虚警率
5. 车辆的检测召回率
6. 车辆的虚警率
7. 总的时效性(推理速度)

> 即每队有 **7 个排名**。各大类召回率/虚警率的算法见 3.1:先算各**子型**的召回率/虚警率,再取平均得到该大类指标(例:船 = 驱护舰 / 航母 / 两栖船 / 民船 四型召回率均值、虚警率均值)。

然后对每个团队 **7 个排名的总和**进行**二次排序**,作为三项打分的重要参考。

### 5.2 打分区间(受二次排序百分比约束)

原则上三项打分的百分比控制在 **(100% − 二次排序百分比)的 ±20%** 以内。

- **初赛**:每项满分 10 分。例:二次排序百分比为第 30% → 每项原则上控制在 **5–9 分**(下限 `(100% − 30% − 20%) × 10`、上限 `(100% − 30% + 20%) × 10`)。每项最低 0 分、最大 10 分。
- **决赛**:同一约束思想,但分值为合理性 20 / 创新 50 / 工程 30。

---

## 6. 其他说明

### 6.1 算法精度评测
评测**可能采用网上评测**,也可能需要到**上海卫星工程研究所(上海市闵行区元江路 3666 号)** 进行线下评测,具体方法后续通知。

### 6.2 评分规则解释权
本打分方案解释权归**赛题发布单位上海卫星工程研究所**所有;如与赛事组委会后续发布方案冲突,以赛事组委会发布的方案为准。

---

## 7. 关键要点速查(给建模/评测实现用)

| 维度 | 要点 |
| --- | --- |
| **三大类** | 舰船、飞机、车辆 |
| **刚性指标(整体,三类合并)** | Recall ≥ 85%、FDR ≤ 20%、单幅 1e4×1e4 ≤ 20s |
| **整体指标计算(官方补充)** | 先分三大类算各自的 Recall/FDR,再对三大类取**算术平均**;大类内部先算各**子型**再取平均 |
| **IoU 阈值** | 车辆 **0.35**;其他(舰船、飞机)**0.50** |
| **匹配规则** | 按 score 降序贪心匹配;一对一;重复框最高分计 TP 其余 FP |
| **推理时间** | 不含数据读取;RTX 3090 或同等国产 GPU/NPU |
| **初赛总分** | 性能 70 + 合理性 10 + 创新 10 + 工程 10 |
| **决赛总分** | 合理性 20 + 创新 50 + 工程 30 |
| **主观分参考** | 7 个排名(3 大类 × 召回/虚警 + 时效性)求和后二次排序,百分比约束打分区间 |
